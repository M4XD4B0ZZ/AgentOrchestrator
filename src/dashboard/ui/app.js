// AO Manager — client behaviour.
//
// Placeholder. This file intentionally does nothing yet: polling the
// /api/snapshot route, rendering into #content, and the AO namespace /
// renderRoute that a later task (client behaviour) wires up all land in a
// subsequent task of this slice. It exists here only so the asset manifest
// in src/dashboard/ui-assets.ts names a complete, loadable set.
