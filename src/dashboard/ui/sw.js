// AO Manager — service worker.
//
// Placeholder. This file intentionally does nothing yet: the install/
// activate cache lifecycle and the shell digest wiring
// (__AO_SHELL_DIGEST__, __AO_SHELL_ROUTES__ substituted at build time) land
// in a later task of this slice. It exists here only so the asset manifest
// in src/dashboard/ui-assets.ts names a complete, loadable set. Nothing
// registers this worker yet, so it never runs.
